# This file can be used to install module depdencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
---
fixtures:
  forge_modules:
    transition: "puppetlabs/transition"
    facter_cacheable: "waveclaw/facter_cacheable"
    boolean: "puppet/boolean"
  symlinks:
    subscription_manager: "#{source_dir}"

